# AutoMIND - local dev quickstart

## Prasyarat
- Python 3.11
- pip
- (opsional) Docker & docker-compose

## Menjalankan secara lokal (tanpa Docker)
1. Buat virtualenv dan install:
```bash
python -m venv .venv
.venv\Scripts\activate     # Windows
pip install -r requirements.txt
